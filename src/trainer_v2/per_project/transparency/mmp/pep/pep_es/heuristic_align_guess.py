from typing import List, Iterable, Callable, Dict, Tuple, Set





class Guess:
    # Tokens are space tokenized (words)
    def guess_align(self, q_tokens: List[str], st: int, ed: int, d_tokens: str):
        for q_token in q_tokens:
            idx = d_tokens.find(q_token)










def main():
    return NotImplemented


if __name__ == "__main__":
    main()